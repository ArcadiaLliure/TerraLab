"""TerraLab's framework-neutral core.

The package is deliberately small at first: it is the stable import home for
model, application, and rendering contracts while compatibility modules are
retired incrementally.
"""
